{
  "name": "mujeres-bridal",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "npm run generate",
    "build": "vite build",
    "preview": "vite preview",
    "generate": "node scripts/generate-catalog.js",
    "watch": "node scripts/watch-catalog.js"
  },
  "devDependencies": {
    "vite": "^5.4.8"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  }
}
